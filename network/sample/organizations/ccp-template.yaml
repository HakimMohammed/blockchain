---
name: test-network-${ORG}
version: 1.0.0
client:
  organization: ${ORG}
  connection:
    timeout:
      peer:
        endorser: '300'
organizations:
  Org${ORG}:
    mspid: ${ORG}MSP
    peers:
    - ${ORG}_peer.${ORG}.enset.com
    certificateAuthorities:
    - ca.${ORG}.enset.com
peers:
  ${ORG}_peer.${ORG}.enset.com:
    url: grpcs://localhost:${P0PORT}
    tlsCACerts:
      pem: |
          ${PEERPEM}
    grpcOptions:
      ssl-target-name-override: ${ORG}_peer.${ORG}.enset.com
      hostnameOverride: ${ORG}_peer.${ORG}.enset.com
certificateAuthorities:
  ca.org${ORG}.enset.com:
    url: https://localhost:${CAPORT}
    caName: ca-org${ORG}
    tlsCACerts:
      pem: 
        - |
          ${CAPEM}
    httpOptions:
      verify: false
